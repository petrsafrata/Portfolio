import { Car, Languages, Layers, Star, Terminal, User } from "lucide-react";
import { Card } from "../components/ui/Card";
import { Section } from "../components/ui/Section";
import { portfolio } from "../data/portfolio";

export function AboutSection() {
  return <Section id="about" title="O mně" subtitle="Krátce, konkrétně – co přináším a co mě baví stavět."><div className="grid gap-4 md:grid-cols-2"><Card><div className="flex items-center gap-2 text-sm font-semibold"><Layers size={18} />Fokus</div><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-zinc-300/80 text-left"><li>Backendové aplikace v Javě a návrh API (Spring Boot).</li><li>Datové modely, validační schémata, práce s MongoDB/Oracle/PostgreSQL.</li><li>Vyhledávání nad dokumenty, indexace, extrakce obsahu (Tika/OCR/ASR).</li><li>Dockerizace a reprodukovatelné nasazení.</li></ul></Card><Card><div className="flex items-center gap-2 text-sm font-semibold"><Terminal size={18} />Styl práce</div><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-zinc-300/80 text-left"><li>Preferuji čistý návrh, srozumitelnou architekturu a praktickou dokumentaci.</li><li>Rád přeměňuji složité problémy na funkční softwarová řešení.</li><li>Jako vyučující umím věci vysvětlit a dotahovat do funkčního výsledku.</li></ul></Card></div><Card className="mt-4 p-4 text-left"><div className="flex items-center gap-2 text-sm font-semibold"><User size={18} />Rychlé info</div><div className="mt-3 grid gap-4 sm:grid-cols-3"><div><div className="flex items-center gap-1.5 text-xs font-medium text-purple-500"><Languages size={14} />Jazyky</div><div className="mt-1.5 space-y-0.5 text-sm text-zinc-300/80">{portfolio.quickInfo.languages.map((language) => <div key={language.name}>{language.name} · {language.level}</div>)}</div></div><div><div className="flex items-center gap-1.5 text-xs font-medium text-purple-500"><Car size={14} />Řidičský průkaz</div><div className="mt-1.5 text-sm text-zinc-300/80">{portfolio.quickInfo.license}</div></div><div><div className="flex items-center gap-1.5 text-xs font-medium text-purple-500"><Star size={14} />Zájmy</div><div className="mt-1.5 text-sm text-zinc-300/80">{portfolio.quickInfo.interests.join(" · ")}</div></div></div></Card></Section>;
}