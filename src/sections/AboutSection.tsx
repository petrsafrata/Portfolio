import { Car, Languages, Layers, Star, Terminal, User } from "lucide-react";
import { Card } from "../components/ui/Card";
import { Section } from "../components/ui/Section";
import { useLanguage } from "../context/useLanguage";

export function AboutSection() {
  const { portfolio, t } = useLanguage();
  return <Section id="about" title={t.about.title} subtitle={t.about.subtitle}><div className="grid gap-4 md:grid-cols-2"><Card><div className="flex items-center gap-2 text-sm font-semibold"><Layers size={18} />{t.about.focusTitle}</div><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-zinc-300/80 text-left">{portfolio.about.focus.map((item) => <li key={item}>{item}</li>)}</ul></Card><Card><div className="flex items-center gap-2 text-sm font-semibold"><Terminal size={18} />{t.about.workStyleTitle}</div><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-zinc-300/80 text-left">{portfolio.about.workStyle.map((item) => <li key={item}>{item}</li>)}</ul></Card></div><Card className="mt-4 p-4 text-left"><div className="flex items-center gap-2 text-sm font-semibold"><User size={18} />{t.about.quickInfoTitle}</div><div className="mt-3 grid gap-4 sm:grid-cols-3"><div><div className="flex items-center gap-1.5 text-xs font-medium text-purple-500"><Languages size={14} />{t.about.languagesLabel}</div><div className="mt-1.5 space-y-0.5 text-sm text-zinc-300/80">{portfolio.quickInfo.languages.map((language) => <div key={language.name}>{language.name} · {language.level}</div>)}</div></div><div><div className="flex items-center gap-1.5 text-xs font-medium text-purple-500"><Car size={14} />{t.about.licenseLabel}</div><div className="mt-1.5 text-sm text-zinc-300/80">{portfolio.quickInfo.license}</div></div><div><div className="flex items-center gap-1.5 text-xs font-medium text-purple-500"><Star size={14} />{t.about.interestsLabel}</div><div className="mt-1.5 text-sm text-zinc-300/80">{portfolio.quickInfo.interests.join(" · ")}</div></div></div></Card></Section>;
}