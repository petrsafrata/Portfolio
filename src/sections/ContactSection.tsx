import { Briefcase, Github, Mail } from "lucide-react";
import { Card } from "../components/ui/Card";
import { Section } from "../components/ui/Section";
import { portfolio } from "../data/portfolio";
const linkIcons = { Mail, Github, Briefcase };
export function ContactSection() { return <Section id="contact" title="Kontakt" subtitle="Rychlá cesta, jak se spojit."><div className="grid gap-4 md:grid-cols-1"><Card><div className="flex items-center gap-2 text-sm font-semibold"><Mail size={18} />Napiš mi</div><p className="mt-3 text-sm text-zinc-300/80 text-left">Nejrychlejší je email. Můžu poslat ukázky soukromých projektů a domluvit se na detailech.</p><div className="mt-5 flex flex-wrap gap-3">{portfolio.links.map((link) => { const Icon = linkIcons[link.icon]; return <a key={link.label} href={link.href} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-sm hover:bg-white/10"><Icon size={18} /><span className="font-medium">{link.label}</span></a>; })}</div></Card></div></Section>; }